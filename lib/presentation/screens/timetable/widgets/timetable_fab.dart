import 'package:flutter/material.dart';

class TimetableFab extends StatelessWidget {
  const TimetableFab({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return FloatingActionButton(
      onPressed: () {},
      child: Icon(Icons.add),
    );
  }
}
