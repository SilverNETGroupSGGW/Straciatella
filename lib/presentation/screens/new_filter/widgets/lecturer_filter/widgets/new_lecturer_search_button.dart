import 'package:flutter/material.dart';

class NewLecturerSearchButton extends StatelessWidget {
  const NewLecturerSearchButton({super.key});

  @override
  Widget build(BuildContext context) {
    return IconButton(onPressed: () {}, icon: const Icon(Icons.search));
  }

  // TODO: Implement searching and picking lecturer id
}
